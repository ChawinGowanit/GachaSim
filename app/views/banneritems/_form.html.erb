<%= form_with(model: banneritem) do |form| %>
  <% if banneritem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(banneritem.errors.count, "error") %> prohibited this banneritem from being saved:</h2>

      <ul>
        <% banneritem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :banner_id %>
    <%= form.text_field :banner_id %>
  </div>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
