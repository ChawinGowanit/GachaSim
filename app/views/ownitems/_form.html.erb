<%= form_with(model: ownitem) do |form| %>
  <% if ownitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ownitem.errors.count, "error") %> prohibited this ownitem from being saved:</h2>

      <ul>
        <% ownitem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id %>
  </div>

  <div class="field">
    <%= form.label :sellstatus %>
    <%= form.check_box :sellstatus %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
